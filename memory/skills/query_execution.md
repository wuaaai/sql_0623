# 查询执行技能

## 执行流程

1. 先用 `list_tables` 确认表名
2. 用 SELECT * WHERE ROWNUM <= 1 了解列结构
3. 根据用户需求编写完整 SQL
4. 执行并展示结果

## 错误处理

- SQL 执行失败 → 根据错误信息修正，不要盲目重试
- 列名不存在 → 先查看表结构确认正确的列名
- 表名错误 → 用 list_tables 确认

## 结果展示

- 结果用表格展示
- 超过20行时告知用户总数
- 金额/数值保留2位小数
- NULL 值显示为 NULL
