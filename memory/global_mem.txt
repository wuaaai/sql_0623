# 全局记忆 (L2)

## 达梦语法要点
- 限制行数: WHERE ROWNUM <= N (不是 LIMIT)
- 字符串拼接: 使用 || (不是 CONCAT)
- 查询当前日期: SYSDATE
- 分页: 使用 ROWNUM 子查询
- 表名/列名在SQL中默认大写

## 统计信息 (运行时动态更新)
- Schema: RDYS_PUBLIC_TBS
- 表数量: 执行 list_tables 获取
